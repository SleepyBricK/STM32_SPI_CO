# Анализ записи Intan RHS2116 — канал ch2

Файл: `recording_export_ch2.csv`  
Канал: `ch2`  
Условие теста: канал замкнут с землей и референсом через резистор 10 кОм.

## Короткий вывод

**ch2 лучше, чем ранее проанализированный ch4, но всё равно плохой для теста закороченного входа.**

Главная проблема: уровень шума и артефактов всё ещё слишком большой. Для входа, замкнутого на `GND/REF` через 10 кОм, ожидаемый уровень должен быть порядка **2–5 µVrms**. В этом файле:

- RMS по всему файлу: **22.03 µVrms**
- RMS после удаления первых 5000 отсчётов: **20.35 µVrms**
- робастная оценка шума через MAD: **13.88 µV**
- есть повторяющиеся отрицательные артефакты до **−579 µV**
- есть стартовый переходный процесс до **+809 µV**
- есть конфликт частоты дискретизации в заголовке файла

Это не выглядит как нормальный шум исправно замкнутого входа RHS2116.

---

## Данные из заголовка CSV

| Поле | Значение |
|---|---:|
| `nominal_sample_rate_hz` | 40000 |
| `measured_sample_rate_hz` | 363628.8617646242 |
| `time_binding_source` | recv_wall |
| `firmware_adc_rate_hz` | 350000 |
| `stream_mux_channel_count` | 1 |
| `channels` | ch2 |
| `rows` | 3783000 |

Важный момент: в заголовке есть конфликт.

С одной стороны:

```text
nominal_sample_rate_hz: 40000
```

С другой стороны:

```text
measured_sample_rate_hz: 363628.8617646242
firmware_adc_rate_hz: 350000
time_binding_source: recv_wall
```

Поэтому частотный анализ нужно трактовать осторожно. Надёжнее сейчас смотреть не на герцы, а на **периодичность в sample index**.

---

## Основная статистика

| Параметр | Значение |
|---|---:|
| Количество отсчётов | 3 783 000 |
| Длительность по `ch2_time_s` | 10.40346 с |
| Среднее | +1.58 µV |
| Медиана | +0.78 µV |
| STD | 21.98 µV |
| RMS | 22.03 µV |
| Минимум | −578.76 µV |
| Максимум | +809.055 µV |
| Peak-to-peak | 1387.815 µV |
| Робастная оценка шума через MAD | 13.88 µV |

---

## Распределение значений

| Порог | Количество точек |
|---|---:|
| `abs(value) > 10 µV` | 1 753 318 |
| `abs(value) > 20 µV` | 605 883 |
| `abs(value) > 50 µV` | 33 500 |
| `abs(value) > 100 µV` | 12 225 |
| `abs(value) > 500 µV` | 661 |
| `abs(value) > 1000 µV` | 0 |

Положительный момент: в ch2 нет милли-вольтовых выбросов выше ±1000 µV, которые были в ch4.

Плохой момент: **661 точка выше ±500 µV** на закороченном входе — это всё равно ненормально.

---

## Статистика после удаления стартового участка

Если убрать первые 5000 отсчётов:

| Параметр | Значение |
|---|---:|
| RMS | 20.35 µV |
| Среднее | +1.39 µV |
| Медиана | +0.78 µV |
| Минимум | −578.76 µV |
| Максимум | +181.35 µV |
| `abs(value) > 100 µV` | 10 224 |
| `abs(value) > 500 µV` | 284 |

То есть стартовый переходный процесс ухудшает картину, но **не объясняет основную проблему**. После его удаления RMS всё равно слишком высокий.

---

## Стартовый переходный процесс

В начале файла есть большой положительный выброс/установление.

Пример:

```text
sample 27  ≈ +518.5 µV
sample 32  ≈ +631.4 µV
sample 40  ≈ +784.9 µV
sample 49  ≈ +809.1 µV
```

Это похоже на:

- включение/перезапуск канала;
- установление AC-coupled high-gain тракта;
- установление референса;
- переходный процесс DSP/HPF-фильтра;
- начальную невалидную часть буфера.

Этот участок нельзя использовать для оценки шума.

---

## Самая важная находка: артефакт каждые 8190 отсчётов

В данных есть повторяющиеся отрицательные провалы с периодом **8190 samples**.

### Ранняя серия

Позиции:

```text
sample = k * 8190 + 20
sample = k * 8190 + 21
```

Диапазон:

```text
sample 8210 ... 1056531
```

По времени CSV:

```text
0.02258 с ... 2.90552 с
```

Примеры:

```text
sample 8210   ≈ −511.29 µV
sample 16400  ≈ −578.76 µV
sample 16401  ≈ −529.23 µV
sample 24590  ≈ −514.61 µV
sample 32780  ≈ −554.58 µV
```

### Поздняя серия

Позиции:

```text
sample = k * 8190 + 1840
sample = k * 8190 + 1841
```

Диапазон:

```text
sample 1705360 ... 3777431
```

По времени CSV:

```text
4.68984 с ... 10.38815 с
```

Примеры:

```text
sample 3744670  ≈ −511.10 µV
sample 3752860  ≈ −513.63 µV
sample 3769240  ≈ −512.85 µV
sample 3777430  ≈ −466.44 µV
```

Это очень похоже не на аналоговую проблему, а на **структурный цифровой/буферный артефакт**.

Период 8190 подозрителен, потому что он близок к буферному размеру 8192. Отличие на 2 отсчёта — отдельный красный флаг.

---

## Частотная картина

Из-за конфликта частоты дискретизации интерпретация частот неоднозначна.

Если использовать `measured_sample_rate_hz ≈ 363628.86 Hz`, период 8190 samples даёт:

```text
363628.86 / 8190 ≈ 44.4 Hz
```

Основные пики:

```text
44.39 Hz
88.78 Hz
133.16 Hz
177.55 Hz
```

Если использовать номинальные 40 kHz, тот же период даёт:

```text
40000 / 8190 ≈ 4.88 Hz
```

Основные пики:

```text
4.88 Hz
9.77 Hz
14.65 Hz
19.53 Hz
```

Вывод: пока timestamp/export не исправлен, частотные выводы вторичны. Надёжный факт — **периодический артефакт каждые 8190 отсчётов**.

---

## Сравнение с ch4

| Параметр | ch2 | ch4 |
|---|---:|---:|
| Количество отсчётов | 3 783 000 | 5 280 876 |
| RMS | 22.03 µV | 62.07 µV |
| Минимум | −578.76 µV | −986.7 µV |
| Максимум | +809.06 µV | +3398.46 µV |
| `abs(value) > 100 µV` | 12 225 | 331 826 |
| `abs(value) > 500 µV` | 661 | 10 531 |
| `abs(value) > 1000 µV` | 0 | 490 |

**ch2 заметно лучше ch4**, но это не значит, что ch2 хороший. Он просто менее сломан.

---

## Ожидаемый уровень для 10 кОм на REF/GND

По даташиту RHS2116 типичный input-referred noise AC high-gain усилителя составляет около **2.4 µVrms**. Также Intan указывает, что резистор **10 кОм** даёт примерно **1.3 µVrms** теплового шума при полосе 10 кГц.

Оценка суммарного ожидаемого шума:

```text
sqrt(2.4^2 + 1.3^2) ≈ 2.7 µVrms
```

Фактически в ch2:

```text
22.03 / 2.7 ≈ 8.2 раза выше ожидаемого
```

Даже робастная оценка:

```text
13.88 / 2.7 ≈ 5.1 раза выше ожидаемого
```

Это слишком много для закороченного входа.

---

## Наиболее вероятные причины

### 1. Ошибка потока / буфера / парсинга

Самая вероятная причина.

Признаки:

- период 8190 отсчётов;
- фазовый сдвиг артефакта: сначала `+20`, потом `+1840`;
- повторяемость с точностью до sample index;
- амплитуда провалов слишком большая для нормального шума закороченного входа.

Проверить:

- границы DMA/USB/SPI-буферов;
- выравнивание 16/32-битных слов;
- не теряются ли 1–2 байта на границе пакета;
- не читается ли служебное слово как ADC sample;
- не смешиваются ли command/status/result words;
- не ломается ли кольцевой буфер GUI;
- нет ли overflow/underrun;
- нет ли ошибки в экспорте CSV.

### 2. Неверная временная модель

`time_binding_source: recv_wall` — плохая основа для анализа реальной частоты.

Нужно, чтобы время считалось так:

```text
time_s = sample_index / hardware_sample_rate
```

А не по wall-clock времени приёма пакетов на ПК.

### 3. Аналоговая помеха / земля / питание

Даже если убрать явные цифровые артефакты, базовая робастная оценка около **13.9 µV** всё равно выше ожидаемой.

После исправления потока нужно проверить:

- шум AVDD/DVDD;
- развязку питания RHS2116;
- `ref_elec`;
- `stim_GND`;
- общую землю;
- петли земли;
- расположение SPI рядом с входами;
- включены ли стимуляторы;
- включены ли charge recovery цепи;
- нет ли длинного провода к REF/GND.

---

## Что делать дальше

### Шаг 1. Не анализировать биосигналы по этим данным

Сейчас это невалидная запись для оценки входного шума. Сначала нужно убрать цифровые/буферные артефакты.

### Шаг 2. Сохранить raw ADC words

Нужен экспорт не только `voltage_uV`, а исходные слова:

```text
sample_index
raw_16bit_word
raw_32bit_spi_result
channel_id
frame_id
packet_id
```

Особенно важно посмотреть сырые слова около:

```text
8210
16400
24590
32780
1056530
1705360
3777430
```

### Шаг 3. Проверить границы буферов

Проверить, что происходит на периоде:

```text
8190 samples
```

Особенно подозрительно, если в коде есть буферы:

```text
8192 bytes
8192 samples
4096 words
16384 bytes
```

### Шаг 4. Исправить timestamp

Нужно убрать `recv_wall` как источник времени для sample-level анализа.

Правильный вариант:

```text
time_s = sample_index / fs
```

где `fs` — реальная аппаратная частота выборки канала.

### Шаг 5. Повторить тест

Настройки теста:

- только один канал;
- вход через 10 кОм на REF/GND;
- стимуляция выключена;
- charge recovery выключен;
- стабильная аппаратная частота;
- запись raw words;
- минимум 10 секунд записи;
- отдельно сохранить первые 20 000 raw samples.

---

## Итог

**ch2 не такой плохой, как ch4, но он всё равно ненормальный.**

Основные признаки:

1. RMS около **22 µV**, что примерно в **8 раз выше ожидаемого**.
2. Есть повторяющиеся отрицательные провалы до **−579 µV**.
3. Артефакт повторяется каждые **8190 отсчётов**.
4. В заголовке конфликтуют `nominal_sample_rate_hz`, `measured_sample_rate_hz` и `firmware_adc_rate_hz`.
5. Канал нельзя считать валидной проверкой аналогового шума, пока не исправлен поток данных.

Главная рекомендация: **сначала чинить цифровой поток, буферизацию и временную модель. Аналоговую часть трогать вторым этапом.**

---

## Источники

- Intan RHS2116 Datasheet: типичный input-referred noise 2.4 µVrms, шаг AC high-gain ADC 0.195 µV, ADC до 714 kS/s, 16 каналов более 40 kS/s каждый.
- Intan Application Note “Noise Reduction Techniques”: шум резистора 10 кОм около 1.3 µVrms при полосе 10 кГц.
